// Cloud Functions entry point. Trusted server logic (battle resolution, reward
// granting, quest completion validation, daily/weekly resets) is exported from
// here as it's built out under battle/, quest/, rewards/, and daily/.
//
// Nothing is implemented yet — this is the skeleton phase. No function is
// exported until there's real logic to back it, so there's nothing to
// accidentally deploy.
export {}
